# Schema pins

Vendored copies under `schemas/` are what hosting CI validates against.

| Schema | Pin |
|--------|-----|
| USP `profile.json` / `usp.json` | commit `e350b19f4c608d5f92ac0d9c2136e8175eb6ec05` |
| UCP `ucp.json` (plus capability / payment_handler / service) | tag `v2026-04-08` (`a2d8bf0b8f5a6fc790f677899c2c7da0684fe33d`) |

This repository validates the committed `platform-profile.json`. It does not author the document.
