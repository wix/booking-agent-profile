# USP Agent platform profile - ownership

Canonical profile URI (platform identity; do not change lightly):

`https://profile.booking-agent.dev/platform-profile.json`

| Asset | Owner |
|-------|-------|
| Domain `booking-agent.dev` | Wix DNS / domain registrant for `booking-agent.dev` |
| GitHub repo `booking-agent-profile` | Public org [`wix`](https://github.com/wix/booking-agent-profile) (seeded from personal history; not a GitHub Transfer ownership) |
| GitHub Pages site (interim until custom domain) | `https://wix.github.io/booking-agent-profile/platform-profile.json` (source: `main` / root; GitHub Actions enabled) |
| Personal cutover copy | [`yahalomran/booking-agent-profile`](https://github.com/yahalomran/booking-agent-profile) Pages `https://yahalomran.github.io/booking-agent-profile/platform-profile.json` remains live until org Pages serves identical bytes |
| Profile document content | `yahalomran/linkusp-cli` `platform_profile_doc()` (SoT). This repo only publishes generated bytes. |

## Who can transfer what

- **Domain transfer / registrar changes:** domain registrant for `booking-agent.dev` (Wix Domains).
- **Repo admin / transfer:** `wix` org owners (this repo). Do not recreate; prefer history-preserving moves.
- **DNS `profile` CNAME:** whoever can edit Wix DNS for `booking-agent.dev`. Target after org Pages is live: `wix.github.io` (host name `profile` only; do not edit apex or `www` A records).
- **GitHub org domain verification TXT:** `wix` org Settings -> Pages -> Verified domains for `booking-agent.dev` (`_github-pages-challenge-wix`). Remove leftover `_github-pages-challenge-yahalomran` if present.

## Prior host

Through 2026-08-17 the issue tracked `usp-agent.dev`. On 2026-08-18 the canonical apex switched to `booking-agent.dev` (see issue [#114](https://github.com/wix-private/universal-scheduling-protocol-spec/issues/114) fourth-pass amendment). Do not bind or document `profile.usp-agent.dev` for this profile.

## Step 10 note (2026-08-20)

Issue #114 preferred GitHub Transfer ownership into a public Wix org. The destination used instead is an empty public `wix/booking-agent-profile` seeded by pushing the personal `main` history (tip `5ecc6d941a433b3bc8caceb29760bfa8cb5e3514`). Canonical URI is unchanged.

## Rotation of neither

This document is **keyless**. There are no profile signing keys to rotate. PoP keys are per-credential and ephemeral in the client; they are never published here.
